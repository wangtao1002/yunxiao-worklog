# 交接说明（2026-08-22 凌晨）

## 现在是什么状态

插件**已经可以装、可以用**了。`云效工时统计-v0.1.0.zip` 就在这个目录里。

- 装法：`chrome://extensions` → 打开右上角「开发者模式」→「加载已解压的扩展程序」→ 选这个目录
- 用法：打开云效工作项页面，底部会出现合计条；按 `Alt+H` 或点插件图标开统计面板

## 已经验证过的（都是在你的真实云效数据上跑的）

| 项 | 结果 |
|---|---|
| 列表页合计条 | 11 个视图逐一核对，数字与云效侧边栏**完全一致** |
| 项目页合计条 | 366 + 14 已取消 = 380，与云效「共 380 条」一致 |
| 字段自动探测 | 零硬编码，运行时准确探出 `101586/101587/79/80` |
| 团队统计 | 一人一次请求，实测前端组 5 人几秒出数 |
| 逾期率 | 算出 18 条，与云效页面自己打的「逾期」标签数**正好相同** |
| 导出 | CSV 有 BOM + 公式注入防护；Markdown 含任务清单可直接贴日报 |
| 空/错误/未登录/大数据量/亮暗主题 | 全部有妥当处理 |

自动化测试 **228 项断言**，并且做过变异验证（故意打断三处关键逻辑，测试全部咬住）。

## 还差最后一件事：写入链路

「批量补填实际工时」现在**只能预演，不能真写**。原因是云效的写入端点是我从它前端 bundle 里
扫出来的路由常量，请求体形状靠反推，**没有实证过**。

我不想拿你的线上工作项去试候选端点——每个失败的候选都是一次真实写请求，
万一某个形状被云效部分接受，就会写进错误的值。

**更安全的办法（需要你两分钟）：**

1. 在浏览器里登录云效（ego-browser 里的登录态过期了，我把控制权交还给你了）
2. 回我一句「继续」
3. 我挂上抓包钩子后，你在云效界面上**手动**改一次某个任务的「实际工时」
   （用你本来就要填的任务就行，不用专门造废数据）
4. 我录下真实的端点和请求体，把它设成第一个候选——第一次真实写入就能命中，
   全程零试错请求

抓包脚本已经写好放在 `docs/API-VERIFY.md` 最后一节。

## 目录里都有什么

```
manifest.json / background.js / options.html / options.js / src/ / icons/   ← 插件本体（打进 zip）

打包（两种产物，都从同一套 src/ 出）：
  python3 tools/pack.py                    → 两个 zip（同事安装 / 商店上传）
  python3 tools/build-userscript.py --host http://…  → 油猴脚本 .user.js（自带自动更新）
油猴版的差异全部由 src/gm-shim.js（把 GM_* 伪造成 chrome.*）和 src/userscript-boot.js
（补上 background 和独立设置页的活）吸收，业务代码两版共用，不要 fork。
离线验证：tools/preview.html（扩展版）/ tools/userscript-preview.html（油猴版）
docs/API-RESEARCH.md      云效内部接口实测文档（含权限边界、字段结构）
docs/API-VERIFY.md        第二轮实证补丁（system 视图坑、filter 转换、写入验证方案）
docs/SPEC.md              模块契约
docs/STORE-LISTING.md     商店上架文案 + 审核必填项
README.md / PRIVACY.md    给同事看的说明 / 隐私政策（中英双语）
tools/smoke-test.mjs      228 项断言，`node tools/smoke-test.mjs`
tools/preview.html        离线预览（假数据驱动真实面板，不用登云效，可拿来给同事演示）
tools/gen_icons.py        图标生成脚本
```

## 几个值得知道的坑（都已经修了，但别改回去）

1. **云效内置视图的 `spaceIdentifier` 是字面量 `"system"`**，不是 userId。直接拿去查恒返回 0 条。
2. **视图 filter 里的 `value` 必须 unwrap**（元素是 `{label,value}`）。不 unwrap 会静默返回 0 条、不报错。
3. **逾期只能看 `finishTime`，不能看状态名**。状态叫「开发完成」的任务 `finishTime` 是 null，
   其实没结束——按状态名判逾期率会从 69.2% 掉到 3.8%，绩效自评会填错。
4. **`sum*` 开头的字段是父子任务 rollup**，选中会让工时翻倍，探测时必须排除。
5. **字段名要认繁体**（工時/實際），否则插件对台港团队直接失效且不报错。
