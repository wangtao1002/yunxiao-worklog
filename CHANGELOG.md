# 更新日志

版本号写在 `manifest.json`，构建脚本从那里读；油猴靠 `@version` 判断要不要给用户推更新。
发版步骤见 [docs/RELEASING.md](docs/RELEASING.md)。

---

## 0.3.2

**新增**

- 设置页增加「任务状态范围」：可统计全部任务或仅统计已完成任务；完成状态按真实完成时间判断，统一作用于面板、日历、分组、详情和悬浮汇总。
- 设置页增加「达标工时口径」：工作日目标可按每条任务的预计/实际最大值、仅预计工时或仅实际工时计算。

**变更**

- 任务状态筛选只影响本地统计，不会缩小写回云效时的任务范围。
- 兼容 0.3.1 的界面裁剪规则：切换统计展示口径时，会同步过滤不适用的悬浮概览项并使用对应口径计算日均工时。

## 0.3.1

**变更**

- 「统计口径」从「换个算法」升级成「**整体裁剪界面**」：用不到的那个字段不再出现在任何地方——
  概览卡、明细表的列、合计条、Markdown 导出、分组统计的数值，全都跟着口径走。
- 「偏差」卡（实际 − 预计）只在「两者都看」时出现。只用一个字段时它没有意义。
- 「按预计工时一键填充」只在用到实际工时时才显示——它写的是实际工时。
- 概览卡名和明细列名改用**运行时探测到的真实字段名**，公司字段叫「研发工时」就显示「研发工时」。
- CSV 导出**不跟着裁**，始终两列。导出是为了存档和二次分析，宁可多给。

**修复**

- 设置页「统计口径」下拉是一排空白：`fillSelect` 吃的是 `[value, label]` 数组对，写成了对象。
  已加断言盯住，以后写成对象直接测试失败。
- 悬浮条显示项里的「未填预计」没跟着口径改名（实际口径下该叫「未填实际」）。

## 0.3.0

**新增**

- **统计口径可配置**：设置页新增「预计工时 / 实际工时 / 两者都看」，默认预计。
  决定热力图着色、日均工时、工作日偏差、未填提醒、分组排序拿哪个字段当基准。
- 未填工时提醒的判据跟着口径走——用实际工时统计的团队，「未填预计」对他们是噪音。

**变更**

- ⚠️ **修正一处早就存在的口径矛盾**：「工时偏差」「截止今日工时偏差」原本写死用**实际工时**，
  而「日均工时」用**预计**，两张卡放一起看根本不是一回事。现在都跟随设置。
  因此默认（预计）口径下这两张卡的数字会和 0.2.x 不同——**那是修正，不是改坏**。

## 0.2.1

**修复**

- 合并 #1 后引入的回归：**改成员后界面不动**。改成员的权威源被换成了回读存储，
  而写入是异步的，`resolveScope` 读到没落盘的旧值又把用户刚点的选择覆盖回去。
  现在界面上的选择才是权威，存储只是它的持久化副本。
- 成员相关的四个入口（只看我 / 不含我 / 勾同事 / 勾自己）改回真加载。
  缓存是按成员组合存的，换了人必然不命中，停在「加载此区间」等用户再点一次，
  对用户来说就是「取消了自己却没反应」。切区间、切口径、自定义日期仍走缓存优先。

## 0.2.0

来自 [@xiayukun](https://github.com/xiayukun) 的 [PR #1](https://github.com/wangtao1002/yunxiao-worklog/pull/1)。

**新增**

- **法定节假日日历**（2023–2026，纯本地数据，不请求第三方）：工作日计算会扣掉法定假期、算上调休补班。
- 概览新增四张工时目标卡：**工作日总工时**、**工时偏差**、**截止今日工时**、**截止今日工时偏差**
  （后两张只在本周 / 本月出现）。工作日总工时 = 工作日数 × 每日标准工时 × 选中人数。
- **区间数据本地快照**：按「组织 + 成员 + 口径 + 起止日期」缓存，按 `savedAt` 保留最近 12 个；
  跨自然日后首次访问自动刷新本月；工时写回后同步修正命中的快照。
- **悬浮条显示项可自定义**：空选沿用旧样式；自定义时「范围」固定显示，切区间会自动剔除不适用项。

**变更**

- 切换时间范围改为缓存优先 + 手动「加载此区间」，减少对云效的重复请求。
- 概览卡分两行：工作日类指标放第二行。

## 0.1.8

**新增**

- **成员选择可以取消自己**：「我」在通讯录列表里成为一个可勾选条目，
  底部另加「只看我」/「不含我」两个快捷按钮。想只看某位同事的工时，把自己的勾去掉即可。
  不含自己时成员摘要变成橙色的「不含我 · XXX」；最后一个人不允许被取消。
- 新增 [docs/RELEASING.md](docs/RELEASING.md)：发版流程、Greasy Fork 的坑、改署名要动哪几处、
  以及网络可达性的正确测法（必须禁代理，带代理测的结论是反的）。

**修复**

- 一处竞态：`onToggleSelf` 改完 state 立刻 `load()`，而 `ensureConfig()` 每次都从存储重读，
  在异步写入落盘前把刚点的选择顶了回去。

## 0.1.7

首个开源版本，也是首个发布到 Greasy Fork 的版本。

**新增**

- **未填预计工时告警**：统计范围里没填工时的任务标红并置顶，概览多一张可点的卡片，
  上方一条警示条说明本区间漏了几条，列表页合计条上也显示条数。
  字段没识别出来时整套提醒自动静默——那时全表都是 0，提醒出来就是整表误报。

**其他**

- 从 Chrome 扩展源码派生出**油猴脚本**版本，两版共用同一套 `src/`：
  `src/gm-shim.js` 把 `GM_*` 伪造成 `chrome.*`，`src/userscript-boot.js` 补上 background 与独立设置页的活。
- 声明 MIT 许可证。

---

## 0.1.6 及更早

在本仓库开源之前，没有 git 历史可考。那时已经具备的能力：
列表页合计条、统计面板（概览卡 / 日历热力图 / 分组统计 / 明细表）、团队对比、
导出 Markdown 与 CSV、批量补填工时（默认只读预演）、字段运行时探测、设置页。
